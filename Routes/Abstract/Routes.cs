namespace minimalistArchitecture.Abstract
{
    /// <summary>
    /// An abstract class that contains all routes of a context
    /// </summary>
    public abstract class Routes
    {

        /// <summary>
        /// This method is used to register all routes of a context.
        /// Called in Routes/RouteManager.cs
        /// </summary>
        /// <param name="app"></param>
        public static void RegisterRoutes(WebApplication app)
        {
            // register all routes of a context
            throw new NotImplementedException();
        }
    }
}