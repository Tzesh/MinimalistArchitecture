namespace MinimalistArchitecture.Abstract
{
    /// <summary>
    /// An abstract class that contains all routes of a context
    /// </summary>
    public abstract class Service
    {
        // create constructor
        public Service(WebApplication app)
        {
            // register routes in the constructor
        }
    }
}